name: 'Git Lint'
description: 'Git commit message linter'
inputs:
  git-lint-config-file:  # id of input
    description: 'Path to git-lint.yml config file'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    LINTER_WORKSPACE_CONFIG_FILE: ${{ inputs.git-lint-config-file }}